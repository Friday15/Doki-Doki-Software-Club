using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class MiniMapScript : MonoBehaviour {
    //public MCControl MC;
    //public Transform map1;
    //public Transform map2;
    //public Transform map3;
    //public Transform map4;
    //public Transform map5;

    void LateUpdate () {
        
        //if (MC.stage == 1)
        //    transform.position = new Vector3(map1.position.x, map1.position.y, transform.position.z);
        //else if (MC.stage == 2)
        //    transform.position = new Vector3(map2.position.x, map2.position.y, transform.position.z);
        //else if (MC.stage == 3)
        //{
        //    if (MC.rg.position.x < map3.position.x - 4)
        //        transform.position = new Vector3(map3.position.x - 4, map3.position.y, transform.position.z);
        //    else if (MC.rg.position.x > map3.position.x + 4)
        //        transform.position = new Vector3(map3.position.x + 4, map3.position.y, transform.position.z);
        //    else
        //        transform.position = new Vector3(MC.rg.position.x, map3.position.y, transform.position.z);
        //}
        //else if (MC.stage == 4)
        //    transform.position = new Vector3(map4.position.x, map4.position.y, transform.position.z);
        //else
        //    transform.position = new Vector3(map5.position.x, map5.position.y, transform.position.z);
    }
}
